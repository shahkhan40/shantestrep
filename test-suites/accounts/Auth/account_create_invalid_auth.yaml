authors:
  - Asiya

endpoint: /accounts
method: POST
auth: NONE
severity: Critical
category: Security_UnSecured

testCases:
  - id: 1
    body:
      '{
        "name": "Fx-{{@Random | 6}}",
        "accountType": "GitHub",
        "accessKey": "testing accessKey",
        "secretKey": "testing secretKey"
     }'
       
      }'
assertions:
  - "@StatusCode == 401"

tags:
  - V2

policies:
  logger: DEBUG
