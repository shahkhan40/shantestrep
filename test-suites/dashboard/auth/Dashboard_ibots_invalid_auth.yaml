---
# Validates project findAll.

authors:
  - Intesar Shannan Mohammed

endpoint: /dashboard/count-ibots
method: GET
auth: NONE
severity: Critical
category: Security_UnSecured
# Auth required for this endpoint.

assertions:
  - "@StatusCode == 401"

tags:
  - V2

policies:
  logger: DEBUG
