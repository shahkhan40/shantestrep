---
# Validates vault listings with params.

authors:
  - Intesar Shannan Mohammed

init:
  - Microsoft_VSTS_Git_create_init
  - project_create_valid_Microsoft_VSTS_Git_create_init_init

endpoint: /projects?page=0&size=10

method: GET

assertions:
  - "@StatusCode == 200"
  - "@Response.errors == false"
  - "@Response.data != EMPTY"

tags:
  - V2
